//Variables in JS

let mystring = "my name"
console.log(`the type is ${typeof(mystring)} and values is ${mystring}`)

let bool = true
console.log(`the type is ${typeof(bool)} and values is ${bool}`)

let mynull = null
console.log(`the type is ${typeof(mynull)} and values is ${mynull}`)

let myundef = undefined
console.log(`the type is ${typeof(myundef)} and values is ${myundef}`)

